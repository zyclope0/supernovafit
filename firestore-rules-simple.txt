// RÈGLES TEMPORAIRES POUR SUPERNOVAFIT
// À copier dans Firebase Console > Firestore Database > Rules

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Autoriser tous les utilisateurs connectés à tout faire
    // ATTENTION: À remplacer par des règles plus sécurisées en production
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 